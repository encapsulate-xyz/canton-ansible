---
# maintains file name, binary names, and anything which is not a directory or path

# config file names
wallet_config_file_name: config.js

wallet_environment_general_file_name: "{{ service_identifier }}.env"
